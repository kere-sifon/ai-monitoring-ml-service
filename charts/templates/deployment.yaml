apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "ml-service.fullname" . }}
  namespace: ai-monitoring
  labels:
    {{- include "ml-service.labels" . | nindent 4 }}
    tier: backend
spec:
  # Always set replicas explicitly. (HPA template not implemented - autoscaling values reserved for future use)
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      {{- include "ml-service.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "ml-service.selectorLabels" . | nindent 8 }}
        tier: backend
    spec:
      # Pod Security "restricted" (K8s 1.28+ / OpenShift): seccomp + non-root
      securityContext:
        seccompProfile:
          type: RuntimeDefault
        runAsNonRoot: true
        {{- if .Values.openshift.enabled }}
        # restricted-v3 (OpenShift 4.18+ / K8s 1.33+): must be explicit, not null
        hostUsers: false
        {{- if .Values.openshift.securityContext.fsGroup }}
        # Omit fsGroup unless your namespace SCC allows this GID (fixed 1000 often fails on restricted-v2)
        fsGroup: {{ .Values.openshift.securityContext.fsGroup }}
        {{- end }}
        {{- end }}
      serviceAccountName: {{ include "ml-service.serviceAccountName" . }}
      automountServiceAccountToken: false
      {{- with .Values.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      containers:
        - name: {{ .Chart.Name }}
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag | default .Chart.AppVersion }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - name: http
              containerPort: 8000
              protocol: TCP
          env:
            {{- range $key, $val := .Values.env }}
            - name: {{ $key }}
              value: {{ $val | quote }}
            {{- end }}
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL
          livenessProbe:
            {{- toYaml .Values.livenessProbe | nindent 12 }}
          readinessProbe:
            {{- toYaml .Values.readinessProbe | nindent 12 }}
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
          {{- if .Values.persistence.enabled }}
          volumeMounts:
            - name: models
              mountPath: /app/models
          {{- end }}
      {{- if .Values.persistence.enabled }}
      volumes:
        - name: models
          persistentVolumeClaim:
            claimName: {{ include "ml-service.fullname" . }}-pvc
      {{- end }}
